spring:
  main:
    banner-mode: off

simulation:
  failure:
    enabled: false
    rate: 0.0
  delay:
    enabled: false
    min-ms: 0
    max-ms: 0

management:
  endpoints:
    web:
      exposure:
        include: health
